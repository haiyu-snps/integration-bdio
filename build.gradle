buildscript {
    repositories {
        jcenter()
        mavenCentral()
        maven { url 'https://plugins.gradle.org/m2/' }
    }
    dependencies { classpath "com.blackducksoftware.integration:common-gradle-plugin:0.0.+" }
}

project.ext.moduleName = 'com.synopsys.integration.integration-bdio'
project.ext.javaUseAutoModuleName = 'true'

version = '17.0.1-SNAPSHOT'

apply plugin: 'com.blackducksoftware.integration.library'

final def versionFile = new File("${projectDir}/src/main/resources/com/synopsys/integration/bdio/version.txt")
versionFile.mkdirs()
versionFile.delete()
versionFile << version

dependencies {
    api 'com.blackducksoftware.integration:integration-common:16.0.0-SNAPSHOT'

    testImplementation 'org.mockito:mockito-core:2.18.3'
    testImplementation 'org.skyscreamer:jsonassert:1.5.0'
}
